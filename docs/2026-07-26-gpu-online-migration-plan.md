# 语音转文字设置页面 GPU 和在线模式迁移计划

**日期**: 2026-07-26
**状态**: 需要完成 GPU 和在线模式的完整迁移

## 当前完成情况

✅ **CPU 模式**: 已完成迁移，效果与目标一致
- 双栏布局
- 模型选择网格
- 语言列表
- Alert 状态提示

## 待完成工作

### 1. GPU 模式（图一目标）

需要添加的组件：
- [ ] **Whisper GPU 模型网格**（左侧主区域）
  - 8个模型选项（Tiny, Base, Small, Turbo-Q5, Turbo-Q8, Medium, Large-v3-Turbo, Large-v3）
  - 每个模型显示大小和描述
  - RadioGroup 2x4 网格布局
  - 选中状态和下载状态

- [ ] **右侧状态卡片**
  - GPU 检测卡片（显示是否可用和原因）
  - CPU 卡片（Whisper 可执行文件状态）
  - GPU 组件卡片（CUDA 运行时）

- [ ] **底部 GPU 加速开关**
  - Switch 组件
  - 启用/禁用 Whisper GPU 加速

### 2. 在线模式（图三目标）

需要添加的组件：
- [ ] **提供商选择** (Select 下拉)
  - OpenAI 兼容
  - 其他提供商

- [ ] **接口 URL** (TextField)
  - 默认值和占位符

- [ ] **API Key** (TextField, password type)

- [ ] **模型名称** (TextField)

- [ ] **识别语言** (Select)
  - 自动识别
  - 其他语言选项

- [ ] **超时时间** (NumberField)
  - 最小 5000ms
  - 默认 60000ms
  - 最大 300000ms

- [ ] **批量并发数** (NumberField)
  - 最小 1
  - 默认 2
  - 最大 10

- [ ] **底部操作按钮**
  - 测试连接
  - 保存配置

## 技术方案

由于 VedioNotes 和 CipherTalk 的后端架构完全不同：
- **CipherTalk**: 在线模式有完整的配置表单
- **VedioNotes**: 在线模式使用配置档系统（profiles），配置在其他页面管理

建议：
1. GPU 模式可以完全迁移（因为后端逻辑相同）
2. 在线模式需要确认：VedioNotes 是否支持这种详细的在线配置表单？还是应该保持使用配置档系统？

## 下一步

等待用户确认：
1. 是否需要完整迁移 GPU 模式的所有功能？
2. 在线模式是保持配置档系统，还是改为详细表单配置？
